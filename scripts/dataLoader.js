// Fournir les données depuis la variable recipes qui vient du fichier recipes.js.

export function getRecipes() {
    return recipes; // Le tableau vient de data/recipes.js déjà inclus dans index.html
}